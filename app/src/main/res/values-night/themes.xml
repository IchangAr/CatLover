<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CatLovers" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/pastel_green</item>

    </style>
    <style name="Theme.CatLovers.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/pastel_green</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/pastel_green</item>
    </style>
</resources>